version: 0.2

phases:
  install:
    commands:
      - echo Installing...
  build:
    commands:
      - echo Building project...
      - mkdir output
      - cp index.html output/

artifacts:
  files:
    - '**/*'
  base-directory: output
